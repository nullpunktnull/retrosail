# SQLite path relative to the prisma/ folder
DATABASE_URL="file:./dev.db"

# Do NOT set NODE_ENV here.
# If NODE_ENV=development is present during `next build`, the build fails
# with /_global-error useContext (known Next.js 16 issue).
