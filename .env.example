DATABASE_URL="file:./dev.db"

# Shared site gate (server-only; never import into client code)
# SITE_PASSWORD="SantisSailorsAllStars"
# SITE_LEARNER_PASSWORD="SantisScrumSailors"
# SITE_UNLOCK_TOKEN="retrosail-unlocked-v1"
# SITE_LEARNER_UNLOCK_TOKEN="retrosail-learner-v1"

# Do NOT set NODE_ENV here.
# NODE_ENV=development during `next build` breaks Next.js 16 (/_global-error).
